<h1>Created Student Accounts</h1>

<p>The following student accounts have been created. These credentials are shown for demonstration/testing purposes only.</p>

<table border="1" cellpadding="6">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Password</th>
    </tr>
  </thead>
  <tbody>
    <% @created.each do |item| %>
      <tr>
        <td><%= item[:name] || '-' %></td>
        <td><%= item[:email] %></td>
        <td><%= item[:password] %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p style="color:red;"><strong>Note:</strong> This page is for demonstration only. Do not share these credentials publicly.</p>
